{
  "name": "api.prismaorm.typescrypt.postgressql",
  "version": "1.0.0",
  "description": "## Descrição",
  "main": "server.ts",
  "prisma": {
    "schema": "./src/external/prisma/schema.prisma"
  },
  "scripts": {
    "dev": "ts-node-dev --respawn src/server.ts",
    "build": "rimraf ./dist && tsc",
    "start": "npm run build && node dist/server.js",
    "seed": "npx prisma db seed --preview-feature",
    "migrate": "npx prisma migrate dev --name init"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/hapi__hapi": "^20.0.12",
    "@types/hapi__joi": "^17.1.8",
    "@types/node": "^18.7.13",
    "prisma": "^4.2.1",
    "ts-node": "^10.9.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.2"
  },
  "dependencies": {
    "@hapi/boom": "^10.0.0",
    "@hapi/hapi": "^20.2.2",
    "@hapi/joi": "^17.1.1",
    "@prisma/client": "^4.2.1",
    "date-fns": "^2.29.2"
  }
}
